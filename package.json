{
	"name": "supertest_poc",
	"version": "1.0.0",
	"description": "This is a POC for supertest tool",
	"main": "index.js",
	"scripts": {
		"test": "mocha test/specs/*.spec.js --timeout 20000 --reporter mochawesome",
		"test:ci": "mocha test/specs/*.spec.js --timeout 20000 && mocha test/specs/*.spec.js --reporter json > reports/test-results.json"
	},
	"keywords": [
		"supertest"
	],
	"author": "Faisal Khatri",
	"license": "ISC",
	"devDependencies": {
		"chai": "^4.3.4",
		"mocha": "^8.4.0",
		"supertest": "^6.1.3"
	},
	"dependencies": {
		"mochawesome": "^7.0.1"
	}
}
